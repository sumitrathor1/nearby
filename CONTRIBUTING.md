# Contributing to NearBy

Thank you for your interest in contributing to **NearBy**! 🚀  
We welcome contributions that improve the platform and enhance the student experience.

---

## 🚀 Getting Started

Follow these steps to set up the project locally:

1. Fork the repository.
2. Clone your fork:
git clone https://github.com/your-username/nearby.git

3. Navigate into the project directory:
cd nearby


4. Set up the local environment:
- Install and run XAMPP / WAMP / MAMP (or any PHP server).
- Import the database file from the `database/` folder into MySQL.
- Update database credentials inside the `config/` folder if required.
- Start Apache and MySQL services.

5. Create a new branch from `main` before making changes.

---

## 🌿 Branch Naming Convention

Use clear, meaningful branch names:

- `feature/issue-number-description`
- `fix/issue-number-description`
- `docs/issue-number-description`
- `refactor/issue-number-description`

### Examples

- `feature/issue-92-empty-state-ui`
- `docs/issue-94-contributing-guide`

---

## 📝 Commit Message Guidelines

Follow consistent commit message prefixes:

- `feat:` – New feature
- `fix:` – Bug fix
- `docs:` – Documentation update
- `refactor:` – Code improvement
- `style:` – UI or formatting changes

### Example
feat: add No Results Found UI (fixes #92)


---

## 🔄 Pull Request Guidelines

Before submitting a Pull Request:

- Ensure your branch is up to date with `main`.
- Create a separate branch for each issue.
- Link the related issue (e.g., `Fixes #94`).
- Provide a clear and concise PR description.
- Avoid unrelated changes in the same PR.
- Resolve all merge conflicts before submission.

---

## 🏷 Issue Labels

Common labels used in this project:

- `bug` – Something isn't working
- `enhancement` – New feature or improvement
- `documentation` – Documentation-related changes
- `ECWoC26` – ECWoC 2026 contribution
- `SWoC26` – SWoC contribution

---

## 📌 Code Style & Best Practices

- Maintain consistent formatting and indentation.
- Follow the existing project structure.
- Avoid unnecessary file modifications.
- Keep code clean and readable.
- Test your changes before submitting a PR.
- Do not push directly to the `main` branch.

---

## 🎯 ECWoC Contribution Workflow

If contributing under ECWoC:

1. Comment on the issue you want to work on.
2. Wait for assignment from maintainers.
3. Create a new branch for that issue.
4. Follow naming conventions and commit guidelines.
5. Mention `Fixes #issue-number` in your PR.
6. Ensure your PR follows all project contribution rules.

---

Thank you for contributing to **NearBy** and helping students find a place they can call home. 🌟